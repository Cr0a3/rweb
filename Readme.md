# Rweb

My take on a rusty react

### Example

To build the example (`examples/simple.rs`), run:
```bash
wasm-pack build --example simple --target web
```

Then you can just open `index.html` in your browser of choice.